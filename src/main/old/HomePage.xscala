package userlogin.pages

import scalatags.Text.TypedTag
import scalatags.Text.all.*

object HomePage {
  def generate(): TypedTag[String] =
    val headHtml = head()

    val bodyHtml = body(
      h1("Hello world")
    )

    html( headHtml, bodyHtml)

}
